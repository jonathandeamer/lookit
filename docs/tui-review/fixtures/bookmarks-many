# Second fixture for docs/tui-review: a plausible user's shelf, used by the
# start-many-bookmarks scene at the end of tour.tape. The default fixture
# (xdg/lookit/bookmarks) ships one bookmark so every other still is
# deterministic; this one exists to show BOOKMARKS carrying real weight.
#
# Chosen to exercise all three row shapes at once:
#   - two communities, which drop out of COMMUNITIES when pinned
#   - two service children, whose hosts then render a structural parent
#   - one target matching no catalog entry, so its note column is blank
jonathan@tilde.team
@cosmic.voyage
@plan.cat
bonsai@flanigan.us
quake@bbs.airandwave.net
someone@example.org
